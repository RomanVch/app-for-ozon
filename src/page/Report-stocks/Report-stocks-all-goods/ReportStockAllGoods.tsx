import './style.css';

export const ReportStocksAllGoods = () => {
  return (
    <div className="stocks">
      {/*          {data.map((item) => (
                <MenuItem name={item.name} id={item.id} stock={item.total} />
            ))}*/}
    </div>
  );
};
